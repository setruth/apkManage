export type ProjectInfo = {
    projectId: string,
    name: string
}
export type APKInfo={
    version: string,
    versionCode: number,
    updateContent: string,
    updateTime: number,
    projectId:string
}
